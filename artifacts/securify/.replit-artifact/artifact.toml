kind = "web"
previewPath = "/"
title = "Securify"
version = "1.0.0"
id = "artifacts/securify"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21778

[services.development]
run = "pnpm --filter @workspace/securify run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/securify", "run", "build" ]
publicDir = "artifacts/securify/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21778"
BASE_PATH = "/"
